/* eslint-disable no-unused-vars */


import {
    directory,
    is_exist_dir
} from  './utils'


export default function (current_dir, options) {
    return "file list"
    // if (is_exist_dir(current_dir)) {
        
    // } else {
    // }
}

