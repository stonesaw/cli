/* eslint-disable no-unused-vars */


import {
    directory,
    is_exist_dir
} from  './utils'


export default function (input, current_dir) {
    return "file list"
    // if (is_exist_dir(current_dir)) {
        
    // } else {
    // }
}

